=begin
A difference between arrays and hashes:
an array uses indices that are only integers to access its values, but 
a hash can use keys of various types (like symbols, strings, integers, etc.)
to access its values.
=end 
